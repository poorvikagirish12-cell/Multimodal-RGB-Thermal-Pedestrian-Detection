# Saved Models
This directory contains the saved PyTorch model weights (checkpoints) generated after training the CMAF-SOEM QFDet model.

The `.pth` files generated here are used by the inference scripts to run detections on new images.
